fn main() {
    /* Expressions have values. Statements don’t.
     * Rust is what is called an expression language.
     * This means it follows an older tradition, dating
     * back to Lisp, where expressions do all the work.
     */
}
